# 2026-09-25 验证记录

环境：本机 `npu312`，Python 3.12.14。未启动新算法的 100 case 全量搜索。

1. `python -m unittest discover -s NPU_9/tests -v`：5 项通过。覆盖超时子进程终止、失败不覆盖 incumbent、预算耗尽后不再生成候选、配对超时重试、结构覆盖/无环、合并时 B/L2 来源一致性。
2. `analysis/validation_focus`、`validation_fusion`：case_005、064、071、086 的五核 A/B/L2 有限搜索，结构及配对检查通过。分数详见《优化与验证说明》。
3. `analysis/validation_large`：case_014、076 五核有限验证，6 个方案通过检查。强结果保留，case_076 B/L2 改进。
4. `analysis/assembled_verified`：整理原有结果并补齐必要配对，不开展候选搜索。100 个 case × 4 个核数 × 3 个问题，共 1,200 个计划全部通过检查；只新增 2 次官方配对评估，无错误、无待补配对。
5. `analysis/final_smoke`：真实 CLI 运行、相同目录续跑、追加精修预算、画图通过。普通续跑前后所有 job 文件哈希保持相同。
6. case_064 两核、五核的 6 个所选方案官方重放一致，两个 B/L2 配对重放一致。
7. `analysis/final_smoke` 与 `analysis/final_shard` 合并到 `analysis/final_merged` 成功，`final_export` 官方两字段计划导出成功。
8. 最后调整了预算耗尽时的生成器停止位置；重跑上述 5 项测试通过，`analysis/release_smoke` 的最终 CLI 小范围运行完整通过。

`assembled_verified/reports/aggregate.csv` 的五核结果，各问题均为 100 case：

| 场景 | 均值 |
|---|---:|
| A | 3.680987029431362 |
| B | 4.053821932218170 |
| L2 | 4.195587843583030 |

`checked_plans=1200` 是完整性、官方计划结构、分数记录和配对检查，不是将 1,200 个所选方案重新模拟。复用分数有官方代码/配置/输入/计划哈希与原始评分记录的验证链。

这些数字没有达到用户提出的 4.0 / 4.5 / 4.5，不能用少量改进或子集均值声称达标。后续正式全量使用 README 中的新建运行命令；开发目录保留当时源码身份，作为只读验证证据。

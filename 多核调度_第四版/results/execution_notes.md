# 执行记录

本次运行以 4 个进程启动，完成前 15 个用例的 60 组配置后，确认机器为 24 逻辑核心、约 32GB 内存，改为 12 个进程续跑。源码没有改变，完成的 60 组直接复用；未完成的组重新计算。运行清单 run_parameters 中 workers=4 是创建时设置，续跑实际使用 12。

bound_a.py 是运行开始后开发的下界实验模块，未接入此次全量候选筛选。本次报告不能声称使用了该模块加速或剪枝。